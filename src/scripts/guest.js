import { displayGuestName } from "./name.js";


document.addEventListener("DOMContentLoaded", () => {
    console.log("DOM fully loaded, running displayGuestName()");
    displayGuestName(); // Run when the page loads
});
